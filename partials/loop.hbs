{{!-- This is the post loop - each post will be output using this markup --}}
{{#foreach posts}}
<div class="post-title">
  <h4><a href="{{url}}">{{title}}</h4>
  <h5>
    <span class="small">{{author}} at</span>
    <time class="small" datetime="{{date format="YYYY-MM-DD"}}">{{date format="YYYY MMM DD"}}</time>
    <span class="small">{{tags prefix=" | "}}</span>
    <span class="small"> | </span>
    <span class="small"><a href="{{url}}#disqus_thread" data-disqus-identifier="{{id}}"></a></span>
  </h5>
</div>
{{/foreach}}

{{!-- Previous/next page links - displayed on every page --}}
<div class="center">
  {{pagination}}
</div>
